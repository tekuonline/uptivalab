NODE_ENV=development
API_PORT=8080
WEB_PORT=4173
DATABASE_URL=postgresql://uptivalab:uptivalab@postgres:5432/uptivalab
REDIS_URL=redis://redis:6379
JWT_SECRET=change-me-to-something-longer-than-16-chars
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=uptivalab
SMTP_PASS=super-secret
APPRISE_URL=http://apprise:8000

# Synthetic Monitoring Browser Configuration
# USE_REMOTE_BROWSER=false - Uses embedded browsers in API container (default, recommended)
# USE_REMOTE_BROWSER=true - Uses separate playwright service (requires uncommenting playwright service in docker-compose)
USE_REMOTE_BROWSER=false
# PLAYWRIGHT_WS_ENDPOINT is only needed when USE_REMOTE_BROWSER=true
PLAYWRIGHT_WS_ENDPOINT=

DOCKER_SOCKET_PATH=/var/run/docker.sock
VITE_API_URL=http://localhost:8080
API_UPSTREAM=http://api:8080

# CORS Configuration (optional)
# Comma-separated list of allowed origins for API requests
# If not set, the API will allow requests from any origin (less secure but flexible)
# For production, set this to your frontend domain(s)
# Example: ALLOWED_ORIGINS=https://uptivalab.yourdomain.com,https://www.yourdomain.com
# ALLOWED_ORIGINS=
